$schema: 'https://moonrepo.dev/schemas/project.json'

project:
  name: 'markdown'
  description: 'DeltaKit markdown streaming renderer'

stack: frontend
language: typescript

tasks:
  build:
    command: 'pnpm'
    args: ['exec', 'tsup']
    deps:
      - 'core:build'
    inputs:
      - 'src/**/*'
      - 'tsup.config.ts'
      - 'tsconfig.json'
    outputs:
      - 'dist'

  dev:
    command: 'pnpm'
    args: ['exec', 'tsup', '--watch']
    type: 'run'

  check:
    command: 'pnpm'
    args: ['exec', 'biome', 'check', 'src/']
    inputs:
      - 'src/**/*'
      - 'biome.json'

  test:
    command: 'pnpm'
    args: ['exec', 'vitest', 'run']
    inputs:
      - 'src/**/*'
      - 'tests/**/*'
      - 'vitest.config.ts'

  publish:
    command: 'pnpm'
    args: ['publish', '--access', 'public', '--no-git-checks']
    deps:
      - '~:build'
      - '~:test'
