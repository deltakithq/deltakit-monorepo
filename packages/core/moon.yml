$schema: 'https://moonrepo.dev/schemas/project.json'

project:
  name: 'core'
  description: 'DeltaKit core library'

stack: frontend
language: typescript

tasks:
  build:
    command: 'pnpm'
    args: ['exec', 'tsup']
    deps: []
    inputs:
      - 'src/**/*'
      - 'tsup.config.ts'
      - 'tsconfig.json'
    outputs:
      - 'dist'

  dev:
    command: 'pnpm'
    args: ['exec', 'tsup', '--watch']
    type: 'run'

  check:
    command: 'pnpm'
    args: ['exec', 'biome', 'check', 'src/']
    inputs:
      - 'src/**/*'
      - 'biome.json'

  test:
    command: 'echo'
    args: ['No tests for core package']

  typecheck:
    command: 'pnpm'
    args: ['exec', 'tsc', '--noEmit']
    inputs:
      - 'src/**/*'
      - 'tsconfig.json'

  publish:
    command: 'pnpm'
    args: ['publish', '--access', 'public', '--no-git-checks']
    deps:
      - '~:build'
